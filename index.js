const urlApi = "https://hp-api.onrender.com/api/characters"

const { createApp } = Vue;

      createApp({
        data() {
          return {
            message: "Hola Harry!",
          };
        },
      }).mount("#app");
